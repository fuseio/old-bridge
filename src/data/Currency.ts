import { ChainId } from '@voltage-finance/sdk-core'
import { nativeOnChain } from '../constants/token'

export const FUSE = nativeOnChain(ChainId.FUSE)
export const BNB = nativeOnChain(ChainId.BNB)
export const ETH = nativeOnChain(ChainId.MAINNET)
